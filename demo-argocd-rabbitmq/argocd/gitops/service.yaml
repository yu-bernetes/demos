apiVersion: v1
kind: Service
metadata:
  name: gitops-webapp-service
spec:
  selector:
    app: webapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
    nodePort: 30005
  type: NodePort
